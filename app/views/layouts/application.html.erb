<!DOCTYPE html>
<html>
  <head>
    <title>Moglog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/chosen/1.8.7/chosen.min.css">
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'public', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <br>
    <header class="sticky-top p-0" >
      <nav class="navbar navbar-expand-lg bg-light">
        <div class="container">
          <a class="navbar-brand" href="/"><%= image_tag asset_path('logo.png'), size: "100x50" %><%= image_tag asset_path('logo-icon.png'), size: "50x50" %></a>
          <div class="collapse navbar-collapse col-xl-8 text-dark" id="navbarNavDropdown">
          <% if end_user_signed_in? %>
          <!--メンバー-->
            <%= render 'layouts/member_header'%>
          <% elsif admin_signed_in? %>
          <!--管理者-->
            <%= render 'layouts/admin_header'%>
          <% else %>
          <!--ゲスト-->
            <%= render 'layouts/gest_header'%>
          <% end %>
        </div>
      </nav>
    </header>

    <main>
      <div class="text-success text-center"><strong><%= flash[:notice] %></strong></div>
      <div class="text-danger text-center"><strong><%= flash[:alret] %></strong></div>
      <%= yield %>
    </main>

    <footer class="text-center">© moglog 2022.06</footer>

  </body>
</html>
