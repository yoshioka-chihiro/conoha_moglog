　<div class="nested-fields">
    <%= hoge.select :food_id, options_from_collection_for_select(Food.all, :id, :name),{prompt: '選択してください'} %>
    <%= hoge.label :quantity, '×' %>
    <%= hoge.select :quantity, *[1..10] %>
    <%= link_to_remove_association '削除', hoge %>
  </div>

